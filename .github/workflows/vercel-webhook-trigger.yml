name: Vercel Webhook Trigger
on:
  repository_dispatch:
    types:
      - 'vercel.deployment.success'
    branches:
      - 'production'
      - 'staging'
      - 'upgrade'
jobs:
  vercel-deploy-action:
    uses: happy-horizon/test/.github/workflows/vercel-deploy-action.yml@production
    secrets: inherit
