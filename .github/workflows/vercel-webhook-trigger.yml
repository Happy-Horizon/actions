name: Vercel Webhook Trigger
on:
  repository_dispatch:
    types:
      - 'vercel.deployment.success'
    branches:
      - 'production'
      - 'staging'
      - 'upgrade'
jobs:
  vercel:
    uses: happy-horizon/action/.github/workflows/horizon-storefront/vercel-deploy-action.yml@production
    secrets: inherit
