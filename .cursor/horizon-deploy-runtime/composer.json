{
    "name": "happy-horizon/horizon-deploy-runtime",
    "description": "Local development runtime for exercising the horizon-deploy toolkit. Not published; provides the Deployer + Hypernode DeployConfiguration classes the toolkit plugs into so deploy.php can be run and tested without the hypernode-deploy Docker image.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "deployer/deployer": "^7.0",
        "hypernode/deploy-configuration": "^3.8",
        "symfony/console": "^6 || ^7",
        "symfony/yaml": "^6 || ^7"
    },
    "config": {
        "allow-plugins": {
            "deployer/*": true
        }
    }
}
